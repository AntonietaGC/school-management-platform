apiVersion: v1
kind: Service
metadata:
  name: school-platform-dev-service
  namespace: school-dev
spec:
  type: NodePort

  selector:
    app: school-platform-dev

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 31080
